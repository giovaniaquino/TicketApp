package com.ticketapp.api.enums;

public class TicketEnum {
    public enum TicketStatus{
        OPEN,
        ON_GOING,
        CLOSED
    }

    public enum TicketPrio {
        LOW,
        MEDIUM,
        HIGH
    }
}
